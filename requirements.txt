# Autonomous Wheelchair v1.0 — runtime dependencies
#
# Versions below are the ones EMPIRICALLY VERIFIED in this repository's audit
# (see docs/architecture.md "Verified environment"). YOLO26-Depth requires
# ultralytics >= 8.4.0; 8.4.138 is the version all audit results were produced with.

# --- Core deep learning ---
torch>=2.4.0
torchvision>=0.19.0

# --- YOLO26 / YOLO26-Depth ---
# YOLO26 model configs (cfg/models/26/) and the `depth` task module
# (models/yolo/depth/) were introduced in the 8.4.x line.
ultralytics>=8.4.0,<9.0.0

# --- Numerics / imaging ---
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
scipy>=1.11.0

# --- Config / IO ---
PyYAML>=6.0
pandas>=2.0.0

# --- Hyperparameter optimization (Optuna, per spec section P) ---
optuna>=3.6.0
# SQLite storage backend for resumable studies
SQLAlchemy>=2.0.0

# --- Plotting / reporting ---
matplotlib>=3.7.0
seaborn>=0.13.0
tabulate>=0.9.0

# --- Model complexity (GFLOPs / params) ---
ultralytics-thop>=2.0.0

# --- Export / numerical validation (section BG/BH) ---
onnx>=1.16.0
onnxruntime>=1.18.0

# --- Testing / linting (section BL) ---
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.6.0
